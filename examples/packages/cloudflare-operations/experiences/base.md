# Cloudflare operations base experience

- Inspect the current resource before changing it.
- Preserve fields outside the requested change.
- Treat deployment submission as pending until Cloudflare reports completion.
- Keep device-specific corrections in the local experience overlay rather than
  modifying the mainstream feed.
