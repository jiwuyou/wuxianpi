# Cloudflare Operations example

This directory demonstrates one Composite Package with several independently
bindable contributions. The repository and download URLs use an `example`
namespace intentionally; this is a contract fixture, not a published Package.

The Package has no build step. Its static Web extension, MCP declaration, Skill,
context, mainstream-backed experience, and assistant template are ready for
candidate validation and activation.
