# Packaging

`termux/` builds the independent WuxianPi release artifacts. Source builds are
owned by the Web and Runtime workspaces; packaging consumes already prepared
directories and does not invoke their package managers.

